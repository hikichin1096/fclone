<h1>FaceBookクローン</h1>
<% @facclones.each do |facclone| %>
  <tr>
    <td><%= facclone.content %></td>
    <td><%= link_to '詳細画面へ', facclone_path(facclone.id) %></td>
    <td><%= link_to "編集画面へ", edit_facclone_path(facclone.id) %><td>
  </tr>
<% end %>
