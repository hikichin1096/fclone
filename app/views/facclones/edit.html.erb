<%= form_with(model: @facclone, local: true) do |form| %>
  <div class="blog_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
<%= link_to 'ブログ一覧画面にもどる', facclones_path %>
